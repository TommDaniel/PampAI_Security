# PostgreSQL Configuration
POSTGRES_USER=phishing
POSTGRES_PASSWORD=phishing
POSTGRES_DB=phishing

# API Configuration
API_PORT=8000
CORS_ORIGINS=*

# SMTP Configuration (for email alerts)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=alerts@phishing-guard.local
