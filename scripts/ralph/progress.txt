## Codebase Patterns
- Alert pattern (webhook + email): add send_*_alert(org_id, event) to alerts.py; call both from auto-persist with asyncio.ensure_future(); guard with `if is_phishing and org_id`
- Email alert: uses Python built-in smtplib (no new deps); config via SMTP_HOST/PORT/USER/PASSWORD/FROM env vars; `endpoint` in alert_configs is the recipient email address
- Webhook alert pattern: create alerts.py with send_webhook_alert(org_id, event); call from auto-persist with asyncio.ensure_future(); guard with `if is_phishing and org_id`
- httpx.AsyncClient is available (in requirements.txt); use for async HTTP in alerts system
- phishing-api/app.py uses Pydantic BaseModel + Field for all request/response models
- API is FastAPI (not Flask), running on Uvicorn; lifespan context manager handles startup/shutdown
- DB connection uses SQLAlchemy async (create_async_engine + async_sessionmaker) with asyncpg driver
- DATABASE_URL env var: `postgresql+asyncpg://user:pass@host:5432/db` — defaults to localhost
- DB_ENABLED flag in db.py: init_db() sets True only on successful connect (graceful degradation)
- Migrations SQL in phishing-api/migrations/ — SQLAlchemy also creates tables via metadata.create_all
- Python syntax check: `python3 -c "import ast; ast.parse(open('file').read())"`
- No pip available in devcontainer — cannot install Python packages for runtime testing
- Auth dependency `get_org_id` in auth.py: returns None (anonymous) or org_id string; raises 401 for bad key, 503 if DB down
- Admin org creation: POST /admin/orgs (no auth required for bootstrap); returns api_key
- Auth is OPTIONAL on existing endpoints — anonymous requests (no X-API-Key) are allowed
- log_event() in db.py uses `.returning(id, created_at)` — merge with values dict for full row; created_at is a Python datetime, call .isoformat() for JSON
- Patch `db.log_event` in tests (not `app.log_event`) — endpoint uses `from db import log_event` inside handler
- Auto-persist pattern: guard `if DB_ENABLED:`, call `await log_event(...)`, wrap in try/except — never fail the response
- Batch auto-persist: use `asyncio.ensure_future(_persist_batch())` to avoid blocking response
- Reports endpoint: require auth (401 if no key), then 403 if caller_org_id != path org_id; use dependency_overrides in tests
- SQLAlchemy conditional counts: `func.count(case((table.c.col == val, 1)))` — counts non-null results (requires `case` imported from sqlalchemy)
- Test auth overrides: `app.dependency_overrides[get_org_id] = lambda: async_fn()` — always clear after test with `app.dependency_overrides.clear()`

# Ralph Progress Log
Started: Thu Apr  9 11:13:07 PDT 2026
---

## 2026-04-09 - US-001
- Created phishing-api/db.py with async SQLAlchemy engine, session factory, and three table definitions: organizations, phishing_events, alert_configs
- Created phishing-api/migrations/001_init.sql with full DDL including indexes
- Updated phishing-api/requirements.txt with sqlalchemy[asyncio]>=2.0.0 and asyncpg>=0.29.0
- Updated app.py lifespan to call init_db() on startup and close_db() on shutdown (graceful degradation if Postgres unavailable)
- **Learnings for future iterations:**
  - DB_ENABLED flag in db.py allows API to run without Postgres — check it before any DB operation
  - org_id in phishing_events is nullable (anonymous requests allowed); FK ON DELETE SET NULL
  - alert_configs FK ON DELETE CASCADE (org deletion removes configs)
  - Three indexes on phishing_events: org_id, created_at DESC, is_phishing
---

## 2026-04-09 - US-002
- Created phishing-api/auth.py with get_org_id() FastAPI dependency (X-API-Key header → org_id)
- Added generate_api_key() using secrets.token_hex(32) → 64-char hex
- Added create_org() helper to insert org into organizations table and return the new API key
- Added OrgCreateRequest/OrgCreateResponse Pydantic models to app.py
- Added POST /admin/orgs endpoint to bootstrap organization creation (no auth required)
- Added phishing-api/test_auth.py with unit tests for generate_api_key, get_org_id dependency, and admin endpoint
- **Learnings for future iterations:**
  - Import DB_ENABLED from db.py inside functions (not top-level) so the flag reflects runtime state
  - get_org_id uses `Security(_api_key_header)` where _api_key_header = APIKeyHeader(auto_error=False) — auto_error=False means missing key returns None instead of 401
  - Auth is optional (anonymous requests return None org_id) — consistent with nullable org_id in phishing_events
  - create_org raises Exception on duplicate org_id (unique constraint) — catch and convert to 409 in endpoint
  - Tests use AsyncMock for session context manager pattern: mock_cm.__aenter__ = AsyncMock(return_value=mock_session)
---

## 2026-04-09 - US-003
- Added `log_event()` helper to phishing-api/db.py: inserts into phishing_events table using `.returning(id, created_at)` and returns a dict with all values + id + created_at
- Added `EventCreateRequest` and `EventCreateResponse` Pydantic models to app.py
- Added `POST /events` endpoint to app.py: optional auth via `get_org_id` dependency, DB_ENABLED guard, event_type validation ('url'|'email'), delegates to `log_event()`, returns 201 with full event data
- Imported `get_org_id` from auth at top of app.py (not lazily) and added `Optional` import to db.py
- Created phishing-api/test_events.py with 10 unit/integration tests covering: 503 on DB disabled, 422 on invalid event_type, 201 for URL/email events, anonymous org_id=None, schema validation, created_at as ISO string
- **Learnings for future iterations:**
  - `phishing_events.insert().values(**values).returning(col1, col2)` returns only the RETURNING columns; merge with original values dict for full row
  - `get_org_id` can be used as `Depends(get_org_id)` in endpoint signatures without lazy import — it's already imported at top of app.py
  - Test pattern: patch `db.log_event` (not `app.log_event`) since the endpoint does `from db import log_event` inside the handler
  - `created_at` from asyncpg/SQLAlchemy RETURNING is a Python `datetime` object; call `.isoformat()` before returning in JSON response
---

## 2026-04-09 - US-004
- Modified `/predict` endpoint: added `org_id: Optional[str] = Depends(get_org_id)` and fire-and-forget `log_event()` after analysis
- Modified `/analyze-email` endpoint: same pattern — optional auth + auto-persist email event
- Modified `/predict-batch` endpoint: optional auth + `asyncio.ensure_future()` to persist each item without blocking response
- Created phishing-api/test_auto_persist.py with tests covering: log_event called when DB enabled, not called when DB disabled, endpoint still returns 200 if log_event raises exception
- **Learnings for future iterations:**
  - Auto-persist pattern: `from db import DB_ENABLED, log_event` inside endpoint handler, guard with `if DB_ENABLED:`, wrap in try/except to never fail the response
  - For batch endpoints, use `asyncio.ensure_future(_persist_batch())` so persistence is truly non-blocking (runs after response is sent)
  - Adding `Depends(get_org_id)` to existing endpoints is backwards-compatible — anonymous requests (no X-API-Key) continue to work with org_id=None
  - To test auto-persist: set `db_module.DB_ENABLED = True`, patch `db.log_event`, call endpoint, assert `mock_log.assert_called_once()`
---

## 2026-04-09 - US-005
- Added `get_org_summary()` to phishing-api/db.py: single-query aggregation using `func.count(case(...))` for conditional counts, `func.avg`, `func.max` on phishing_events filtered by org_id
- Added `case` and `select` to SQLAlchemy imports in db.py
- Added `OrgSummaryResponse` Pydantic model to app.py
- Added `GET /reports/{org_id}/summary` endpoint: requires auth (401 anonymous), 403 on org_id mismatch, 503 if DB disabled, returns summary stats
- Created phishing-api/test_reports.py with 7 tests covering: 503 on DB disabled, 401 anonymous, 403 mismatch, 200 correct schema, ISO string last_event_at, empty org zeros
- **Learnings for future iterations:**
  - SQLAlchemy conditional count pattern: `func.count(case((table.c.col == val, 1)))` — must import `case` from sqlalchemy
  - For protected endpoints where caller must be the resource owner: check `caller_org_id != path_org_id` → 403
  - Test dependency override: `app.dependency_overrides[get_org_id] = lambda: async_fn()` where async_fn is a zero-arg async function returning the mocked value; always `app.dependency_overrides.clear()` in finally
  - `patch("db.get_org_summary", AsyncMock(...))` — patch on the db module, not app module (consistent with log_event pattern)
---

## 2026-04-09 - US-006
- Created phishing-api/alerts.py with `send_webhook_alert(org_id, event)` and `_build_payload()` helpers
- Added `get_alert_configs(org_id, alert_type)` to phishing-api/db.py: queries alert_configs table for enabled entries
- Modified phishing-api/app.py: auto-persist in /predict, /analyze-email and /predict-batch now capture the returned row and fire `asyncio.ensure_future(send_webhook_alert(...))` when `is_phishing=True and org_id is not None`
- Created phishing-api/test_webhook.py with 11 tests covering: POST to configured endpoint, no-op when empty configs, HTTP error graceful handling, config fetch failure graceful handling, multiple endpoints, payload structure, created_at ISO conversion, /predict webhook trigger, /predict no webhook on legitimate, /predict no webhook on anonymous
- **Learnings for future iterations:**
  - httpx.AsyncClient is already in requirements.txt — use for async outbound HTTP (alerts, integrations)
  - Webhook pattern: `asyncio.ensure_future(send_webhook_alert(...))` inside the existing try/except block in auto-persist — fires async without blocking the response
  - auto-persist previously used `await log_event(...)` discarding the return value; for US-006 we capture it as `row = await log_event(...)` then pass to alert
  - Guard webhook with `if is_phishing and org_id:` — anonymous requests (org_id=None) have no alert config to look up
  - `get_alert_configs()` returns [] (not raises) when DB_ENABLED=False — safe to call without DB guard
  - Lazy import pattern: `from alerts import send_webhook_alert` inside the auto-persist block (consistent with `from db import log_event` pattern)
---

## 2026-04-09 - US-007
- Added `send_email_alert(org_id, event)` to phishing-api/alerts.py: fetches `email` type configs from DB, sends SMTP email to each recipient, fire-and-forget with graceful error handling
- Added `_send_smtp(recipient, subject, body)` helper (synchronous, uses Python built-in smtplib + email.message)
- Added `_build_email(payload)` helper that builds subject and plain-text body from the alert payload
- Added SMTP config constants from env vars: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM
- Updated 3 auto-persist spots in app.py (/predict, /analyze-email, /predict-batch) to also call send_email_alert alongside send_webhook_alert
- Created phishing-api/test_email_alert.py with 11 tests covering: sends to single recipient, no-op with no configs, graceful SMTP error, graceful config fetch failure, multiple recipients, subject/body content, /predict integration (trigger/no-trigger)
- **Learnings for future iterations:**
  - Email alert uses Python built-in smtplib — no new requirements.txt entries needed
  - `_send_smtp` is synchronous — called from async context, runs in event loop (acceptable for low-frequency fire-and-forget alerts)
  - `endpoint` field in alert_configs stores email address (recipient) for email alerts, webhook URL for webhook alerts — same column, different semantic
  - Same guard as webhook: `if is_phishing and org_id:` — anonymous events never trigger alerts
  - SMTP_USER/SMTP_PASSWORD: if both set, starttls() + login() is called; otherwise connects anonymously (e.g. localhost relay)
  - Lazy import `from alerts import send_webhook_alert, send_email_alert` inside the auto-persist block — both in same import line now
---
