## Codebase Patterns
- phishing-api/app.py uses Pydantic BaseModel + Field for all request/response models
- Working tree has CRLF line endings on many files (.gitattributes controls this)
- Extension lives in extensao-phishing/, API in phishing-api/
- TypeScript typecheck: `npx tsc --noEmit` from extensao-phishing/
- Python syntax check: `python3 -c "import ast; ast.parse(open('file').read())"`
- No pip available in devcontainer - cannot install Python packages for runtime testing
- Existing cascade architecture: BERT (URL) -> CatBoost (WHOIS/DNS) with confidence thresholds
- Email/translation models load with try/except so API still starts if HuggingFace models unavailable
- MarianMT uses MarianTokenizer + MarianMTModel from transformers (not AutoTokenizer)

# Ralph Progress Log
Started: Sun Mar 29 16:54:11 PDT 2026
---

## 2026-03-29 - US-001
- Added EmailRequest, EmailUrlResult, EmailResponse Pydantic models to phishing-api/app.py
- Files changed: phishing-api/app.py
- **Learnings for future iterations:**
  - Models were already present in working tree from prior work - just needed to commit
  - app.py had CRLF endings causing large diff vs main
  - No pip in devcontainer, use ast.parse for syntax validation
---

## 2026-03-29 - US-002
- Added global variables: email_model, email_tokenizer, translation_model, translation_tokenizer
- Added constants: EMAIL_MODEL_ID, TRANSLATION_MODEL_ID
- Updated load_model() to load DistilBERT email + MarianMT translation after BERT URL + CatBoost
- Both models sent to device and set to .eval()
- Each model loads in its own try/except so API still starts if HuggingFace models are unavailable
- Files changed: phishing-api/app.py
- **Learnings for future iterations:**
  - MarianMT requires MarianTokenizer + MarianMTModel (not AutoTokenizer/AutoModel)
  - Email/translation model loading is wrapped in try/except independently to keep API functional even if models fail to download
  - Import: `from transformers import MarianMTModel, MarianTokenizer`
---

## 2026-03-29 - US-003
- Added `detect_and_translate(text: str) -> tuple[str, str, bool]` function to phishing-api/app.py
- Uses `langdetect.detect()` for language detection, translates PT->EN via MarianMT if available
- Added `langdetect>=1.0.9` to requirements.txt
- Import `from langdetect import detect as langdetect_detect` was already in working tree
- Files changed: phishing-api/app.py, phishing-api/requirements.txt
- **Learnings for future iterations:**
  - CRLF line endings in app.py can cause Edit tool ENOENT errors on first attempt; re-reading the file resolves it
  - langdetect import was already added to working tree from prior work
  - `detect_and_translate` handles edge cases: empty text returns "unknown", langdetect exceptions caught
---
