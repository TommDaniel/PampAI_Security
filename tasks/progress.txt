## Codebase Patterns
- API code lives in `/workspace/phishing-api/app.py` (single-file FastAPI app)
- Model files in `/workspace/phishing-api/model/` (DomURLs-BERT, 422MB safetensors)
- Extension code in `/workspace/extensao-phishing/` using Plasmo framework (Chrome MV3)
- Typecheck command: `npx tsc --noEmit --skipLibCheck` (skipLibCheck needed due to @types/react scheduler/tracing issue)
- Training format: `[URL] <url> [WHOIS] unknown [EXTRA] key=val key=val ...` (see `benchmark-modelos/criar_dataset_multimodal.py`)
- Training used `max_length=128` (see `model/training_metadata.json`) - now correctly set in app.py
- `create_feature_text` accepts `server_features: ServerFeatures` param for full feature integration
- Server-side features in `server_features.py`: WHOIS (cache + live), DNS (MX/NS/SPF/A), HTTP redirects
- Feature order in [EXTRA] must match training: redirects, length, dom_length, dot, hyphen, slash, at, params, shortened, tls, dom_age, dom_expire, mx_servers, nameservers, dom_google_index, dom_spf, dom_in_ip, vowels_domain, srv_client, email
- Features with value -1 (unknown/failed) are omitted from [EXTRA] text
- Python environment in sandbox has no pip/packages - use AST-based structural validation for Python checks
- Node.js 20 available with npm; extension has node_modules already installed
- No network access from sandbox - cannot install packages or reach external services

---

## 2026-03-28 - US-001
- Rewrote `phishing-api/app.py`: new `PhishingRequest` (url + client_features with 11 fields), new `PhishingResponse` (url, is_phishing, confidence, label, analysis, inference_ms)
- Removed all RF/ensemble logic: `analyze_predictions`, `CONFIDENCE_THRESHOLD`, `rf_confidence`, `rf_prediction`, `model_used` fields
- Added CORS middleware (configurable via `CORS_ORIGINS` env var, default `*`)
- Updated `/health` to return `version` and `device`
- Rewrote `test_api.py` with pytest + FastAPI TestClient (mocked model), 14 test cases covering schema, predictions, CORS, and RF removal verification
- Files changed: `phishing-api/app.py`, `phishing-api/test_api.py`
- **Learnings for future iterations:**
  - `create_feature_text` still uses the wrong format (`URL: ... | key: value`) - US-002 must fix to training format `[URL]...[WHOIS]...[EXTRA]...`
  - `max_length=512` left as-is with TODO comment for US-002 to change to 128
  - Sandbox has no pip/pytest - tests validated structurally via AST; full pytest run requires Docker or external env
  - The model's `config.json` says max_position_embeddings=512 but training used max_length=128 - important distinction
---

## 2026-03-28 - US-002
- Fixed `create_feature_text` to match training format: `[URL] <url> [WHOIS] unknown [EXTRA] key=val key=val ...`
- Changed tokenizer `max_length` from 512 to 128 to match training config
- Added `whois_text` parameter to `create_feature_text` (default `[WHOIS] unknown`) for future US-003 integration
- Feature `domain_google_index` not included (was always -1 in training, removed from client features)
- Added 9 new tests in `TestFeatureTextFormat` class validating format against training examples
- Files changed: `phishing-api/app.py`, `phishing-api/test_api.py`
- **Learnings for future iterations:**
  - Training format found in `benchmark-modelos/criar_dataset_multimodal.py` (functions `format_whois` and `format_extra`)
  - ClientFeatures field names already match the abbreviated feature names used in training (e.g., `dom_length`, `shortened`, `tls`)
  - Training used `[WHOIS] unknown` (not `not_found`) for missing WHOIS data - must use same string
  - The `whois_text` param in `create_feature_text` is ready for US-003 to inject real WHOIS data
  - Two dataset sources had different formats: GregaVrbancic had `[EXTRA]` features, Brazilian had `[EXTRA] none` with WHOIS tokens
---

## 2026-03-28 - US-003
- Created `phishing-api/server_features.py` with async WHOIS, DNS, and redirect extraction
- WHOIS: local cache (whois_cache.json, 5798 entries) loaded once at startup, live fallback via python-whois with 3s timeout
- DNS: MX servers, nameservers, SPF (TXT), A record queries via dnspython with 2s timeout each
- HTTP redirects: counted via httpx async client with 5s timeout
- All lookups parallelized with `asyncio.gather`, individual failures return defaults (-1)
- Updated `create_feature_text` to accept `ServerFeatures` and produce correct training feature order (20 features)
- Features with value -1 (unknown/failed) are omitted from [EXTRA] to match training behavior (NaN features were skipped)
- `predict_phishing` is now async and calls `extract_server_features` before inference
- Added `python-whois`, `dnspython`, `httpx` to requirements.txt
- Added 13 new test cases: domain extraction, WHOIS text building, cache lookup, ServerFeatures defaults, graceful failure
- Files changed: `phishing-api/server_features.py` (new), `phishing-api/app.py`, `phishing-api/test_api.py`, `phishing-api/requirements.txt`
- **Learnings for future iterations:**
  - The training data omits features with NaN/None values entirely (not set to -1 or 0), so we omit -1 values from [EXTRA]
  - `predict_phishing` is now async — any future callers must await it
  - The mock in test_api.py patches `app.extract_server_features` to return default ServerFeatures, so predict endpoint tests don't need network
  - WHOIS cache is loaded in-memory once — for US-005 Docker, the cache file must be in the model volume
  - `domain_google_index` is always -1 and always omitted from text (matches training where it was always -1)
---

## 2026-03-28 - US-004
- Added POST `/predict-batch` endpoint accepting `List[PhishingRequest]`, returning `List[PhishingResponse]`
- Batch tokenization: all feature texts tokenized in single `tokenizer()` call, single model forward pass
- Server features extracted in parallel via `asyncio.gather` across all URLs
- Extracted `_build_analysis` helper to share analysis text logic between single and batch predict
- Added 7 test cases: 200 status, correct count, order preservation, schema, 5-URL batch, empty batch, invalid request
- Files changed: `phishing-api/app.py`, `phishing-api/test_api.py`
- **Learnings for future iterations:**
  - Batch tokenizer call uses same params as single: `return_tensors="pt", truncation=True, max_length=128, padding=True`
  - When mocking batch model output, logits tensor shape must be `[N, 2]` where N = batch size
  - `predict_batch_phishing` shares `inference_ms` across all items (total time, not per-URL)
  - Empty batch returns `[]` directly without hitting the model
---

## 2026-03-28 - US-005
- Updated `Dockerfile`: Python 3.11-slim base, added `curl` for healthcheck, added `COPY server_features.py`, removed model COPY (mounted as volume), set `MODEL_PATH` env var, increased `start-period` to 120s for model loading
- Updated `docker-compose.yml`: removed deprecated `version` key, removed stale `CONFIDENCE_THRESHOLD` env var, added `MODEL_PATH` and `CORS_ORIGINS` env vars, model mounted as volume `./model:/app/model`, healthcheck uses `curl` instead of Python `requests`
- Updated `.dockerignore`: exclude test files, Dockerfile, docker-compose.yml, and whois_cache.json from build context
- Files changed: `phishing-api/Dockerfile`, `phishing-api/docker-compose.yml`, `phishing-api/.dockerignore`
- **Learnings for future iterations:**
  - Model is ~422MB so it should be volume-mounted, not COPY'd into the image (keeps image smaller and allows model updates without rebuild)
  - Healthcheck should use `curl` not Python `requests` (requests not in requirements.txt)
  - Python 3.14 is too bleeding edge for torch compatibility — use 3.11 for stability
  - `start-period` of 120s needed because loading the DomURLs-BERT model takes time on first start
  - The deprecated `version` key in docker-compose.yml should be removed (Docker Compose V2+ doesn't need it)
---

## 2026-03-28 - US-006
- Created `extensao-phishing/src/utils/clientFeatures.ts` with `ClientFeatures` interface (11 fields) and `extractClientFeatures(url)` function
- Extracts: length, dom_length, dot, hyphen, slash, at, params, shortened, tls, vowels_domain, email
- `shortened` checks against 15 known URL shorteners (bit.ly, tinyurl.com, t.co, etc.)
- `email` uses regex to detect email patterns in the URL
- Domain extraction for `dom_length` and `vowels_domain` uses simple hostname split (second-to-last part)
- Files changed: `extensao-phishing/src/utils/clientFeatures.ts` (new)
- **Learnings for future iterations:**
  - Extension typecheck requires `--skipLibCheck` flag due to `@types/react` scheduler/tracing type mismatch
  - The old `features.ts` (51 features for GBM/ONNX) is still present — will be deleted in US-013
  - `ClientFeatures` field names match the API's `PhishingRequest.client_features` schema exactly
  - Domain vowel counting uses the registered domain part only (e.g., "google" from "www.google.com")
---

## 2026-03-28 - US-007
- Created `extensao-phishing/src/utils/api.ts` with HTTP client for the DomURLs-BERT FastAPI backend
- `analyzeUrl(url, features)` sends POST to `/predict` with URL + ClientFeatures, returns `ApiResponse` or `{ offline: true }`
- `checkHealth()` sends GET to `/health`, returns `HealthResponse` or `{ offline: true }`
- API URL configurable via `chrome.storage.sync` key `apiUrl` (default: `http://localhost:8000`)
- 5-second timeout on all requests using `AbortController`
- Network errors and non-OK responses return `{ offline: true }` instead of throwing
- Exported types: `ApiResponse`, `ApiOfflineResponse`, `HealthResponse`, `HealthOfflineResponse`
- Files changed: `extensao-phishing/src/utils/api.ts` (new)
- **Learnings for future iterations:**
  - Use `"offline" in response` to distinguish offline responses from successful API responses in consuming code
  - API URL is stored in `chrome.storage.sync` (synced across devices), cache data in `chrome.storage.local`
  - The `ApiResponse` fields match `PhishingResponse` from the Python API exactly (snake_case)
  - `fetchWithTimeout` uses `AbortController` since `fetch` doesn't natively support timeouts
---

## 2026-03-28 - US-008
- Created `extensao-phishing/src/utils/cache.ts` with URL analysis caching via `chrome.storage.local`
- `CacheEntry` interface: isPhishing, confidence, label, analysis, timestamp
- Storage key: `urlCache` in `chrome.storage.local`
- URL normalization: lowercase hostname, remove trailing slash
- TTL: 24h for LEGITIMATE, 7 days for PHISHING results
- Max 5000 entries; evicts oldest 20% when exceeded (LRU eviction)
- Exported functions: `getCached(url)`, `setCached(url, entry)`, `clearCache()`
- Expired entries are cleaned up on read (lazy eviction)
- Files changed: `extensao-phishing/src/utils/cache.ts` (new)
- **Learnings for future iterations:**
  - Cache uses `chrome.storage.local` (persistent, per-device), distinct from `chrome.storage.sync` used for settings like API URL
  - `normalizeUrl` lowercases hostname and strips trailing slash — consuming code should always use `getCached`/`setCached` (never access cache directly)
  - Eviction sorts all entries by timestamp and removes oldest 20% — simple LRU approximation
  - `CacheEntry.timestamp` is set by the caller (not auto-set), giving flexibility to background.ts
---

## 2026-03-28 - US-009
- Rewrote `extensao-phishing/src/background.ts` to orchestrate analysis pipeline: whitelist → blacklist → cache → API
- Pipeline implemented in `analyzePipeline()`: checks whitelist first (instant), then blacklist (instant), then cache (instant), then API call
- Results stored per tab in `chrome.storage.session` under key `tabResults`
- Messages supported: ANALYZE_URL, GET_RESULT, GET_API_STATUS, CLEAR_CACHE, SET_API_URL
- ANALYZE_URL receives `{ url, features }` and returns `{ isPhishing, confidence, label, analysis, source, offline? }`
- Source values: `blacklist | whitelist | cache | api | offline`
- API failure returns fail-open result (`offline: true`, `isPhishing: false`)
- Cache stored in `chrome.storage.local` (via cache.ts), survives Service Worker restarts
- Removed all references to ONNX, inference.ts, PredictionResult
- Exported `AnalysisResult` and `AnalysisSource` types for use by other modules (popup, detector)
- Files changed: `extensao-phishing/src/background.ts`
- **Learnings for future iterations:**
  - `isWhitelisted` is exported from `blacklist.ts` (same file as `isBlacklisted`) — whitelist is embedded in that module
  - Tab results use `chrome.storage.session` (cleared when browser closes), distinct from cache in `chrome.storage.local`
  - `sendResponse` in message listener requires `return true` to keep the port open for async responses
  - `emitNotification` only fires for phishing detections (not for whitelist/cache/offline)
  - GET_RESULT expects `{ tabId }` in the message — popup must send the active tab's ID
---

## 2026-03-28 - US-010
- Updated `extensao-phishing/src/contents/detector.ts` to use the new API-based pipeline
- Replaced `import PredictionResult from inference.ts` with `AnalysisResult from background.ts` and `extractClientFeatures from clientFeatures.ts`
- Content script now extracts 11 client features and sends `{ type: ANALYZE_URL, url, features }` to background
- Phishing → red banner, confidence < 70% → orange banner, legitimate → no banner, offline → no banner (fail-open)
- Fixed confidence threshold from 0.7 to 70 (API returns percentage 0-100, not 0-1)
- Replaced `result.confidencePct` (old string field) with `result.confidence.toFixed(1) + "%"`
- No references to inference.ts or old features.ts
- Files changed: `extensao-phishing/src/contents/detector.ts`
- **Learnings for future iterations:**
  - `AnalysisResult` is exported from `background.ts` (not from a utils file) — content script imports it directly
  - API confidence is 0-100 (percentage), not 0-1 (probability) — all threshold checks must use percentage scale
  - The old `PredictionResult.confidencePct` was a pre-formatted string; new code formats confidence inline
  - Offline detection uses `result.offline` boolean from `AnalysisResult` — check before banner logic
---

## 2026-03-28 - US-011
- Rewrote `extensao-phishing/src/popup.tsx` to use API-based pipeline instead of local ONNX model
- Popup now fetches result from background via GET_RESULT message (no model loading)
- Shows result card: PHISHING (red), LEGÍTIMO (green), or API Offline (orange)
- Shows confidence bar (0-100%), analysis text, and source (blacklist/whitelist/cache/api/offline)
- Header shows API online/offline status indicator (green/red dot) via GET_API_STATUS
- Settings panel with configurable API URL field (calls SET_API_URL) and Clear Cache button (calls CLEAR_CACHE)
- Header subtitle changed to "DomURLs-BERT via API"
- Metrics row shows: result label, source, and "DomURLs-BERT" as analysis engine
- Removed all imports of inference.ts, features.ts, FEATURE_ORDER, PredictionResult, DecisionLog
- Updated popup.css: added API status dot, offline card, settings panel, API URL input styles
- Files changed: `extensao-phishing/src/popup.tsx`, `extensao-phishing/src/popup.css`
- **Learnings for future iterations:**
  - API confidence is 0-100 (percentage), so ConfidenceBar uses value directly (no `* 100` conversion)
  - GET_RESULT expects `{ tabId }` — popup gets active tab ID via `chrome.tabs.query`
  - `AnalysisResult` and `AnalysisSource` types are exported from `background.ts`
  - `chrome.storage.sync` stores API URL (synced), `chrome.storage.local` stores cache (per-device)
  - Settings panel uses transient "saved" state with 2s timeout for user feedback
---

## 2026-03-28 - US-012
- Updated `DecisionLog` interface in `extensao-phishing/src/utils/logger.ts`: added `source: AnalysisSource` field, removed `featuresUsed` field
- Added `AnalysisSource` type export (`"blacklist" | "whitelist" | "cache" | "api" | "offline"`) in logger.ts
- Updated `logDecision` message from "GBM" to "DomURLs-BERT" and added source info
- Fixed `inference.ts` logDecision call to use `source: "api"` instead of `featuresUsed` (keeps typecheck passing until US-013 deletes the file)
- Updated confidence comment from "0-1" to "0-100" to reflect API percentage scale
- Files changed: `extensao-phishing/src/utils/logger.ts`, `extensao-phishing/src/utils/inference.ts`
- **Learnings for future iterations:**
  - `AnalysisSource` is now defined in both `background.ts` and `logger.ts` — they are identical union types; US-013 cleanup could consolidate
  - `inference.ts` still references logger — needed minimal fix to keep typecheck green before US-013 deletes it
  - DecisionLog is only actively used in inference.ts (legacy); new pipeline doesn't call `logDecision` yet — future work could wire it into background.ts
---

## 2026-03-28 - US-013
- Removed `onnxruntime-web` and `tldts` from package.json dependencies
- Removed `wasm-unsafe-eval` from CSP in manifest
- Cleaned `web_accessible_resources` (removed .onnx, .wasm, .js references; kept blacklist.json and Icone.png)
- Deleted `src/utils/inference.ts` and `src/utils/features.ts`
- Deleted all ONNX/WASM assets: modelo_phishing_ort14.onnx, 7 .wasm files, ort.min.js, ort.wasm.min.js, LEIA-ME.txt
- Removed `assets/modelo/` directory entirely
- Added `tsconfig.tsbuildinfo` to `.gitignore`
- Updated package description from GBM/ONNX to DomURLs-BERT via API
- npm install runs without errors, typecheck passes
- Files changed: `extensao-phishing/package.json`, `extensao-phishing/package-lock.json`, `extensao-phishing/.gitignore`, plus 13 deleted files
- **Learnings for future iterations:**
  - `tldts` was only used by `features.ts` — safe to remove after features.ts deletion
  - No other source files imported from `inference.ts` or `features.ts` (all imports were replaced in US-009 through US-012)
  - Extension size reduced dramatically (~95MB → ~26MB) by removing ONNX model and WASM runtime
  - `tsconfig.tsbuildinfo` is a TypeScript incremental build cache — should always be gitignored
---

## 2026-03-28 - US-014
- Created `phishing-api/test_integration.py` with 61 end-to-end integration tests across 14 test classes
- Tests cover all 8 acceptance scenarios: API startup, blacklist hit, whitelist hit, unknown URL, cache hit, API offline, API recovery, popup contract
- Additional validation: Docker config, extension structure, ONNX cleanup verification, API-extension contract, full pipeline flow with confidence bands
- Extension typecheck passes (`npx tsc --noEmit --skipLibCheck`)
- All test classes validated via AST (syntax correct, proper pytest structure)
- Files changed: `phishing-api/test_integration.py` (new)
- **Learnings for future iterations:**
  - Sandbox has no pip/pytest — integration tests validated structurally via AST; full pytest run requires Docker or external env
  - Extension has no test runner (no Jest/Vitest) — typecheck is the primary validation proxy
  - API confidence is 0-1 float (not 0-100 percentage); extension multiplies by 100 for display
  - Docker/Chrome E2E scenarios require manual verification outside sandbox — test code documents expected behavior
  - 61 tests across 14 classes provides comprehensive coverage of the full extension↔API pipeline contract
---
